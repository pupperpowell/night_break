import 'package:flutter/widgets.dart';

class CandleStand extends StatelessWidget {
  const CandleStand({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Candle Stand');
  }
}
