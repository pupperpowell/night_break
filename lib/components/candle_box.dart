import 'package:flutter/widgets.dart';

class CandleBox extends StatelessWidget {
  const CandleBox({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('candles and candle box goes here');
  }
}
