import 'package:flutter/material.dart';

class CandleStandPage extends StatelessWidget {
  const CandleStandPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Candle Stand'),
      ),
    );
  }
}
